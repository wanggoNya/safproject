<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Config 3.0//EN" "http//mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="User">
	<!-- 아이디 중복검사 -->
	<select id="checkId" parameterType="string" resultType="_int">
		select count(userId) from tbl_user 
		where userId = #{userId}
	</select>
	
	<!-- 회원 가입 -->
	<insert id="join" parameterType="userVO">
		insert into tbl_user
		(
			userId, userPw, userName, userAge, userGender, userEmail, 
			userZipcode, userAddress, userAddressDetail
		)
		values
		(
			#{userId}, #{userPw}, #{userName}, #{userAge}, #{userGender}, #{userEmail},
			 #{userZipcode}, #{userAddress}, #{userAddressDetail}
		 )
	</insert>
	
	<!-- 로그인 -->
	<select id="login" parameterType="map" resultType="_int">
		select userNumber from tbl_user where userId=#{userId} and userPw=#{userPw}
	</select>
	
	<!-- 회원 정보 조회 -->
	<select id="getInfo" parameterType="_int" resultType="userVO">
		select 
			userId, userPw, userName, userAge, userGender, userEmail, 
			userZipcode, userAddress, userAddressDetail 
		from tbl_user where userNumber = #{userNumber}			
	</select>
</mapper>













